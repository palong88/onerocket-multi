<div class="row">
  <!-- spaceholder -->
  <div class="col s12 m12 l2">&nbsp;</div>
  <!-- form -->
  <div class="col s12 m12 l8">
    <div class="row top">
      <h4 style="display: inline">Stakeholders</h4>
      <%= link_to '<i class="material-icons left">add</i>Add a Stakeholder</a>'.html_safe, new_stakeholder_path, class: "waves-effect waves-light btn-large", style: "float: right" %>
    </div>
    <div class="row">
      <p>Stakeholders get an email when a new hire gets added to the system with special instructions. For example, you may want to automate sending an email to "Joe" in IT to make sure the new hire has a laptop ready for them on their first day.</p>
    </div>
    <div class="row">
      <ul class="collection">
        <% @stakeholders.each do |stakeholder| %>
        <li class="collection-item avatar">
          <i class="material-icons circle">perm_identity</i>
          <span class="title"><%= stakeholder.name %></span>
          <p><a href="mailto:<%= stakeholder.email %>"><%= stakeholder.email %></a> <br>
          </p>
          <div class="secondary-content">
            <%= link_to '<i class="material-icons">mode_edit</i>'.html_safe, edit_stakeholder_path(stakeholder) %>&ensp;&ensp;
            <%= link_to '<i class="material-icons">delete</i>'.html_safe, stakeholder, method: :delete, data: { confirm: 'Are you sure you want to delete this stakeholder?' } %>
          </div>
          <p class="grey-text"><%= stakeholder.department %></p>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
  <!-- spaceholder -->
  <div class="col s12 m12 l2">&nbsp;</div>
</div>
<p id="notice"><%= notice %></p>
